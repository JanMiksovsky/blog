html = mapDeep(../posts, mdHtml, '.md', '.html'):
templated = mapDeep(html, post.ori):
postsAlias = posts:

posts = mergeDeep(this, templated):
  "[year]":
    index.yaml = html/{{year}}:
    index.html = year.ori(index.yaml):

production = meta(this):
  images = ../images:
  posts = merge(postsAlias, this):
    index.html = postsAlias/index.html:
