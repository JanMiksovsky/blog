= @if @not(_/draft), `
  <article>
    <a class="quiet" href="{{ _/path }}">
      {{ @if(_/title, =`<h1 class="postHeader">{{ _/title }}</h1>`) }}
      <p class="postDate">{{ _/formattedDate }}</p>
    </a>
    {{ _/@text }}
  </article>
`
