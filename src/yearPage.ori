(posts, year) => page.ori({
  title: year
  _body: Tree.indent`
    <h1 class="yearHeader">${ year }</h1>
    ${ Tree.map(
      Tree.filter(posts, { test: (post) => !post.draft })
      postFragment.ori
    ) }
  `
})
