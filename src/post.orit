{{ =@if(@not(_/draft), `
<article>
  <a class="quiet" href="{{ postUrl.js(_) }}">
    <h1 class="postHeader">{{ _/title }}</h1>
  </a>
  <p class="postDate">{{ formatDate.js(_/date) }}</p>
  {{ _ }}
</article>
`) }}
