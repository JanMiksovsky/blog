{{ page.orit(
  @document(
    `
      <h1>Posts by year</h1>
      <ul class="yearList">
      {{ @map/values reversed, =`
        <li class="yearItem">
          <a href="{{ @key }}/">{{ @key }}</a>
        </li>
      `}}
      </ul>
    `
    { title: "Posts by year" }
  )
) }}
